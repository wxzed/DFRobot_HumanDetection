name: CI
on:
  push:
    branches:
      - "**"   # 监听所有分支，避免分支名不匹配导致不触发
  pull_request:
  workflow_dispatch:  # 允许在 Actions 页面手动触发

jobs:
  call-reusable:
    uses: cdjq/cloud-code-checks/.github/workflows/ci.yml@master
    secrets: inherit
